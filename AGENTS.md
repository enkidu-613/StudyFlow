# StudyFlow 代理入口

开始任何 StudyFlow 工程任务前，读取 `.agent/AGENTS.md`，再读取其中指向的 `studyflow-project-guidance` skill。它规定了本项目的计划、实现、环境变量、账户密钥、部署、排障和交付说明格式。

仓库现有的其他技术约束仍然有效；本文件只负责把项目级指导入口暴露给能自动发现根目录 `AGENTS.md` 的代理工具。
